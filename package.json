{
  "name": "astra-2-stock-radar",
  "private": true,
  "version": "0.1.0",
  "description": "Push alerts the moment the REDMAGIC Astra 2 becomes purchasable.",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "typecheck": "pnpm -r --no-bail typecheck",
    "test": "pnpm --filter @astra/worker test",
    "bootstrap": "tsx scripts/bootstrap.ts",
    "probe": "tsx scripts/probe.ts",
    "simulate-restock": "tsx scripts/simulate-restock.ts",
    "deploy:worker": "pnpm --filter @astra/worker deploy"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
